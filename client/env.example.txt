# Rename this file to .env and put it in the client folder.
# Vite only reads it when you start the dev server (npm run dev) or build (npm run build).

# Backend API URL (no spaces around =)
VITE_BACKEND_URL=http://localhost:3000

# For production on Render: set VITE_BACKEND_URL in Render Dashboard
# (e.g. https://your-backend.onrender.com) — do not put secrets in this file.
